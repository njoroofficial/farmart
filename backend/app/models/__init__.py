"""

Models package. All model classes are importable from this package.

The imports here are intentionally left minimal — the actual model files
are imported in create_app() within app/__init__.py. This file just marks
the directory as a Python package.

"""
